buildscript {
    ext {
        buildToolsVersion = "35.0.0"
        minSdkVersion = 29     // Android 10 - Minimum supported version
        compileSdkVersion = 35 // Latest SDK for development
        targetSdkVersion = 35  // Target latest Android for maximum compatibility
        ndkVersion = "27.1.12297006"
        kotlinVersion = "2.1.20"
        hermesEnabled = true  // For backward compatibility with libraries
    }
    repositories {
        google()
        mavenCentral()
        maven { url "https://www.jitpack.io" }
        maven { url "https://maven.google.com" }
        maven { url "https://repo1.maven.org/maven2/" }
    }
    dependencies {
        classpath("com.android.tools.build:gradle")
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("com.google.gms:google-services:4.4.0")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")
    }
}

apply plugin: "com.facebook.react.rootproject"
